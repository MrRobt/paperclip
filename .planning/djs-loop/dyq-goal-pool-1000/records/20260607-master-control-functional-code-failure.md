# 2026-06-07 主控无实质功能提交事故记录

时间：2026-06-07 13:31:33 +0800
记录人：小黑

## 结论

主人反馈成立：当前 10 分钟 Kanban 主控运行时间过长，但没有稳定形成“按原目标树交付的功能性代码提交”闭环。主控主要在巡检、评论、纠偏、扩/改任务口径之间循环，未把完成定义强压到每张执行卡的本地提交号和目标树功能点。

已立即执行止血：暂停主控 cron `049f48f67e7b`，避免继续空转和扩大噪声。

## 现场证据

### 主控状态

- cron：`049f48f67e7b Kanban目标池10分钟主控推进`
- 原状态：active，最近一次运行成功，但实际只推动看板状态/评论链
- 当前状态：paused
- 暂停时间：2026-06-07 13:29:23 +0800

### 看板状态

命名看板：`dyq`

- `t_5cfb7fc2`：done，集成设计与验收矩阵
- `t_1e351242`：running，Claw 云端任务编排与 MQ 解耦最小闭环
- `t_db1a264c`：running，PokeClaw 端侧任务领取执行与截图证据回传
- `t_69f3ecfa`：running，WeFlow 微信事件转云端任务与安全草稿队列
- `t_84586445`：running，管理后台三端商业化监控入口
- `t_02afac1b`：todo，三端商业化闭环验收矩阵

### 仓库证据

1. `/mnt/e/code/dyq`
   - 最近提交：`6ec1376eb feat(claw-biz): goal-pool endpoint + 5 field contract (owner-3)`
   - 该提交来自早前 owner-3，不是当前 r66 执行卡的稳定收口。
   - 当前存在 18 个已跟踪文件未提交功能差异，包含 Claw 设备控制器、任务状态、MQ 发布、设备服务、测试等，说明 worker 有写代码，但主控没有把它们收口成目标树提交。

2. `/mnt/e/code/PokeClaw`
   - 最近提交：`c814288 feat(端云闭环): P1.1-P2.4 端云任务领取 执行结果 截图证据 六类结果结构化`
   - 当前新增未跟踪脚本：`scripts/pokeclaw-p3p1-claim-execute-evidence.sh`
   - 新增脚本未提交。

3. `/mnt/d/work/code/WeFlow`
   - 最近提交：`0e90573 docs(W2.5): 补全 EVIDENCE 提交号`
   - 当前新增未跟踪功能文件：
     - `electron/services/aiDraftGenerator.ts`
     - `electron/services/wechatEventCloudTaskService.ts`
     - `tests/w3-01-event-cloud-task.test.cjs`
   - 功能文件未提交。

4. `/mnt/e/code/ai-ui-admin-vue3aa`
   - 最近提交：`107b5ea7d feat(小龙虾入口摸底): 三主线目标池接入真实接口与入口摸底台账`
   - 当前未提交差异：
     - `src/router/modules/base.ts`
     - `src/views/claw/home/index.vue`
     - `vitest.claw.config.ts`
   - 页面/路由改动未提交。

## 根因

1. 主控把“看板推进/评论/矩阵”当成了主要产出，没有用提交号作为硬门禁。
2. r66 错误扩出 C3/P3/W3/WEB3/QC3 口径，虽然后来映射回原目标树，但任务标题仍容易诱导 worker 继续写矩阵和包装，而非按 C1/C2/P1/P2/W1/W2/S1-S4 直接交付功能。
3. 主控不直接写代码是对的，但没有强制 worker 在完成前提交；导致真实代码停留在未提交区。
4. 定时主控适合巡检/调度，不适合驱动开发执行本身；主人偏好应改为“会话内主控 + 子 Agent/worker 实装 + 只读汇报”。

## 已执行纠偏

1. 已暂停 cron `049f48f67e7b`。
2. 已安装独立技能入口 `ralph-loop`，作为 djs-loop 执行阶段别名。
3. 后续不得继续用该主控空转推进，必须改为提交驱动：每轮只收口一个最小功能闭环，完成即本地提交。

## 后续修复方案

### 第一优先级：收口现有未提交功能差异

按仓库分别收口，不再扩新任务：

1. DYQ 后端：聚焦 `C1/C2`，先把 Claw 设备/MQ/任务接口差异跑最小验证后提交。
2. PokeClaw：聚焦 `P1/P2`，把端侧 claim/execute/evidence 脚本验证后提交。
3. WeFlow：聚焦 `W1/W2`，把微信事件云端任务服务、安全草稿服务和测试验证后提交。
4. 管理后台：聚焦 `S1-S4` 管理台入口，把 claw home/route 改动验证后提交。

### 第二优先级：改执行模式

停止“定时任务驱动开发执行”。改为：

- 当前会话或后台会话启动 Ralph Loop 主控；
- 多仓库泳道并行，但提交串行验收；
- 每轮必须输出一个提交号；
- 只读 cron 最多用于汇报，不再创建/修改任务和评论刷屏。

## 新完成定义

任何卡/轮次要标完成，必须同时满足：

- 映射到原目标树节点；
- 有真实功能代码/配置/页面/脚本改动；
- 有本地提交号，或写清不能提交的阻塞原因；
- 有验证命令与结果；
- 有证据目录；
- 主控亲自复核 `git status`、`git log`、关键测试/接口/页面证据。
